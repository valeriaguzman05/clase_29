import auth from "./auth.js";

function login(){ 
    const user = doument.getElementById("user")
    const password = doument.getElementById ("password")
}

function getLoging(data) { 
    return fetch("localhost:300/users",{
        method: "POST";
        headers: { 
            'content-type : '
        }       
    }
 
}